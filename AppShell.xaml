<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PETS.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:PETS.View"
    xmlns:info="clr-namespace:Microsoft.Maui.ApplicationModel;assembly=Microsoft.Maui.Essentials"
    Shell.FlyoutBehavior="Disabled"
    >

    <!--<Shell.FlyoutHeader>
        <Label Text="MAUI"
               FontSize="30"
               FontAttributes="Bold"
               HorizontalTextAlignment="Center"/>
    </Shell.FlyoutHeader>
    <Shell.FlyoutFooter>
        <VerticalStackLayout Padding="10">
            <Label Text="{x:Static info:AppInfo.Name}"/>
            <Label Text="{x:Static info:AppInfo.VersionString}"/>
        </VerticalStackLayout>
    </Shell.FlyoutFooter>-->
    
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate views:HomeView}"
        Route="HomeView"
        />

    <!--<FlyoutItem Title="COnfiguracion"
                >
        <Tab Title="Mamiferos">
            <ShellContent Title="Mamiferos"
                          ContentTemplate="{DataTemplate views:HomeView}"
                          Route="HomeView"/>
        </Tab>
        <Tab Title="Carnivoros">
            <ShellContent Title="Mamiferos"
                          ContentTemplate="{DataTemplate views:HomeView}"
                          Route="HomeView"/>
        </Tab>
        
    </FlyoutItem>-->

</Shell>
